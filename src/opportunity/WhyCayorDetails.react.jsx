import React from 'react';
import PageDetails from '../layout/pageDetails.react';

const WhyCayorDetails = (props) => (
  <div className="WhyCayorDetails">
    <PageDetails
      pageDetails={props.pageDetails} />
  </div>
)

export default WhyCayorDetails;
