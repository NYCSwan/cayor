import React from 'react';
import PageDetails from '../layout/pageDetails.react';

const EsgStrategyDetails = (props) => (
  <div className="EsgStrategyDetails">
    <PageDetails
      props={props.pageDetails} />
  </div>
)

export default EsgStrategyDetails;
